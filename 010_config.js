//maintaing all events names in a config file. this helps us to over come typo errors and 
//change actual event names in one file only (if necessary)

module.exports = {
    events : {
        GREET : 'greet'
    }
};